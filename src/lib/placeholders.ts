/**
 * ═══════════════════════════════════════════════════════════════════════════
 *  HOME HERO MEDIA
 * ═══════════════════════════════════════════════════════════════════════════
 *  Real Ageless Living brand assets. To swap, drop the new file into
 *  `public/media/` and update the path below — Vite hot-reloads it everywhere.
 *
 *  Per-service and per-location imagery now lives directly in the components
 *  that render it (imported from `src/assets/real/`), so there are no shared
 *  placeholder maps to keep in sync.
 * ═══════════════════════════════════════════════════════════════════════════
 */

export const HERO_VIDEO_MP4 = "/media/hero.mp4";
//  ↑ Real Ageless Living brand hero video, full-screen background on `/`.

export const HERO_POSTER = "/media/hero-poster.webp";
//  ↑ Real clinic still shown before the video loads / on mobile + slow links.
